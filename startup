#!/bin/bash
cd /home/site/wwwroot

# Extract deployment if needed
if [ -f output.tar.gz ]; then
    tar -xzf output.tar.gz
fi

# Start the app directly
python -m gunicorn --bind 0.0.0.0:8000 --worker-class aiohttp.GunicornWebWorker --timeout 600 app:APP